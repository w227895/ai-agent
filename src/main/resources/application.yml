server:
  port: 8080

spring:
  application:
    name: flight-change-mail-agent
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY:}
      chat:
        options:
          model: deepseek-chat
          temperature: 0.2

logging:
  level:
    com.ke.deepseektools.tools: INFO
    com.ke.deepseektools.service: INFO
